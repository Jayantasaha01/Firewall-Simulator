# Firewall Simulator

A simple firewall simulation tool that reads packet logs from the packets.json file and applies simple IP-based rules. It can be modified to a specific ip address or a enge of ip address

## Usage

1. Ensure you have Python 3 installed.
2. Run the simulator:
3. The packets.json file stores the logs of the ip addressess

```bash
python3 firewall.py
